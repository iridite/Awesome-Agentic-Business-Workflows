[project]
name = "ai-trend-sniffer"
version = "0.1.0"
description = "Automated AI Trend Sniffer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "beautifulsoup4",
    "pyyaml",
    "openai",
    "pydantic",
]

[tool.uv]
package = true
